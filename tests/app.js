/*jshint esversion:6*/
var chai = require('chai');
var assert = chai.assert;


//set up testing on the server

describe('tests', () => {
	it('should return true', () => {
		assert.isTrue(true);
	});
});